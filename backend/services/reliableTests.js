import axios from 'axios'

export const comparePhotos = async (passport, selfie) => {
  //Compare images using the api
  //const {data} = await axios.post('/api/example/...',{passport,selfie})

  return true
}
